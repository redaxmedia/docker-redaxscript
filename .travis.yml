services:
 - docker

install:
 - docker-compose build
 - docker-compose up -d

script:
 - docker-compose ps | grep -q redaxscript
 - docker-compose ps | grep -q mssql
 - docker-compose ps | grep -q mysql
 - docker-compose ps | grep -q pgsql
 - docker-compose ps | grep -q smtp

notifications:
 email:
 - team@redaxscript.com
 irc:
 - chat.freenode.net#redaxscript
 webhooks:
 - https://webhooks.gitter.im/e/ff9acda2e5faf42e3182
